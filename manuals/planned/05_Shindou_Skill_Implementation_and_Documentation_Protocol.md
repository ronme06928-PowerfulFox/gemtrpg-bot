**最終更新日**: 2026-03-19
**対象バージョン**: Current
**対象機能**: 新スキル「震盪」(受け手側デバフ版) / スキル拡張時の仕様同期・データ更新ガイド

---

## 1. 目的

本書は次の2点を定義する。

1. 新スキル **「震盪」** の実装案（受け手側デバフとしての実現可能性、実装方式、テスト観点）
2. 今後スキル拡張を行う際に、**仕様書の更新** と **図鑑系データの更新案内** をどう分離して扱うかの運用ルール

本書の重要前提は次のとおり。

- リポジトリ内の仕様書（`manuals/implemented/03`, `08`, `09` など）は実装確定後に更新対象となる
- 一方、用語図鑑・バフ/デバフ図鑑・スキルデータの正本は、原則として **Google Sheets / データベース / キャッシュ更新フロー** 側にある
- したがって、AI支援は **「どのシートのどの列をどう更新するか」を提示するところまで** とし、図鑑系の派生ファイルを直接編集する運用にはしない

既存の提案（主に `manuals/planned/03_*` に記載した案）にも適用する。

---

## 2. 新スキル「震盪」実装案（受け手側デバフ）

### 2.1 要件

- 名称: `震盪`
- 種別: `デバフ`
- 効果: **このデバフを受けた対象が「破裂」をスキル効果で付与される時、増加量を +N する**

---

### 2.2 現状ロジックとの適合性

結論: **中〜高。既存基盤 + 軽微なロジック拡張で実装可能。**

理由:

- `manager/game_logic.py::APPLY_STATE` は正値付与時に `calculate_state_apply_bonus(...)` を呼ぶ
- ただし `calculate_state_apply_bonus(...)` は **実行者(actor)側の `special_buffs` のみ参照** しており、受け手(target)側デバフは参照しない
- よって、要件どおり「付与される側」で判定するには target 側参照を追加する必要がある

補足:

- 効果適用アクションは `APPLY_DEBUFF` ではなく、現行仕様どおり `APPLY_BUFF` を使って target に付与する
- `APPLY_STATE_PER_N` には現状 `state_bonus` 適用処理がないため、震盪対象にしたい場合は同時に拡張する

---

### 2.3 推奨実装方式

### 方式A（推奨）: `state_receive_bonus` を新設

`state_bonus`（付与する側）とは分離し、受ける側専用ルールとして `state_receive_bonus` を導入する。

デバフ定義例:

```json
{
  "name": "震盪",
  "effect": {
    "state_receive_bonus": [
      {
        "stat": "破裂",
        "operation": "FIXED",
        "value": 2,
        "consume": false
      }
    ]
  }
}
```

スキル側（デバフ付与）:

```json
{
  "timing": "HIT",
  "type": "APPLY_BUFF",
  "target": "target",
  "buff_name": "震盪",
  "lasting": 1,
  "delay": 0
}
```

ロジック変更ポイント:

1. `manager/game_logic.py` に `calculate_state_receive_bonus(receiver, source, stat_name, context=None)` を追加
2. `APPLY_STATE` 正値処理で、既存の `state_bonus` に加えて `state_receive_bonus` を加算
3. `APPLY_STATE_PER_N` 正値処理にも同じボーナス加算処理を追加
4. `consume=true` は **受け手(target)側のデバフを消費** する

### 方式B（代替）: `state_bonus` に方向フラグを追加

`state_bonus` へ `applies_on: "source" | "target"` を追加して共通化する方式。

- 利点: キーを増やさずに済む
- 欠点: 既存定義の後方互換と解釈が複雑になる

運用上は、影響範囲を分離しやすい方式Aを推奨。

---

### 2.4 実装時の重要注意点

- 震盪の上乗せは「正の破裂付与」にのみ適用する
  - `破裂-3` のような減少には適用しない
- 適用対象は「スキル効果として処理される付与」に限定する
  - `APPLY_STATE` / `APPLY_STATE_PER_N` 経由
  - `SET_STATUS` など直接代入には適用しない
- 震盪（受け手側）と既存の付与側ボーナスが同時にある場合の扱いを固定する
  - 原則: 合算（例: 付与側+1, 受け手側+2 -> 合計+3）
- `consume=true` の消費元が target であることを厳密に検証する

---

### 2.5 推奨テスト（チェック観点）

- 震盪+2を受けた対象に `破裂+3` を付与すると `+5` になる
- 震盪なしでは `+3` のまま
- 破裂以外（出血、亀裂）には影響しない
- `破裂-3` には上乗せしない
- `consume=true` は1回目のみ上乗せし、2回目は上乗せしない
- `APPLY_STATE_PER_N` 由来の破裂付与にも上乗せされる
- 付与側ボーナスと受け手側ボーナス同時時の合算結果が仕様通り

---

## 3. 実装対象とデータ正本の切り分け

### 3.1 リポジトリ内で実装・更新するもの

- `manager/game_logic.py`
- 必要に応じて戦闘処理・lint・test
- `manuals/implemented/03_Integrated_Data_Definitions.md`
- `manuals/implemented/08_Skill_Logic_Reference.md`
- 必要に応じて `manuals/implemented/09_SelectResolve_Spec.md`
- 必要に応じて `manuals/implemented/01`, `02`, `04`

### 3.2 ユーザーがシート/DB側で更新するもの

- スキルデータ正本
- 用語図鑑データ（Glossaryシート）
- バフ/デバフ図鑑データ（Buff Catalogシート）
- それらに対応するキャッシュ再生成・DB反映

### 3.3 本プロトコルにおけるAI支援の境界

本プロトコルでは、AI支援は次の内容に留める。

- どのシートを更新するべきかの整理
- 必須列・推奨値・入力例の提示
- 更新順序の提示
- DB/キャッシュ反映後の確認項目の提示

逆に、次は **AIが直接実施する前提にしない**。

- 図鑑系シートの実データ入力
- シート正本を元にした図鑑派生ファイルの手動更新
- 「データ図鑑ファイルを直接直して完了」とする運用

---

## 4. データ更新方針（新運用）

### 4.1 基本原則

スキル拡張時の更新対象は、次の2系統に分けて扱う。

1. **仕様・実装系**
   - コード
   - テスト
   - `03/07/08` などの仕様書
2. **データ正本系**
   - Google Sheets などのマスターデータ
   - 必要なDB/キャッシュ更新
   - 画面上での反映確認

このとき、図鑑系データについては **「repo内ファイルを直接編集して合わせる」のではなく、「正本シートを更新して反映させる」** を標準運用とする。

---

### 4.2 更新順序

1. まず `03` と `07` を更新し、必要なら `08` も更新する
2. 実装とテストを完了する
3. AI は、ユーザー向けに「どのシートのどの列をどう更新するか」を提示する
4. ユーザーが Glossary / Buff Catalog / スキル正本シートを更新する
5. ユーザーが通常のデータ更新フローで DB / キャッシュ反映を行う
6. 反映後にゲーム画面・図鑑・説明表示を確認する

---

### 4.3 データ図鑑系の扱い

#### A. Glossary

- 正本は Glossary シート
- 更新対象は、用語ID・表示名・short・long・関連語など
- 反映確認は、Glossary UI と説明リンク表示で行う

#### B. Buff Catalog

- 正本は Buff Catalog シート
- 更新対象は、名称・種別・説明・effect JSON
- 反映確認は、バフ/デバフ図鑑表示と実際のツールチップで行う

#### C. `static/js/buff_data.js` などの派生物

- 直接編集を標準手順にしない
- もし UI 側に静的説明定義が残っていて差分が出る場合でも、まず「シート正本と反映フローが正しいか」を確認する
- 必要な静的追従がどうしても残る場合だけ、別タスクとして扱う

---

## 5. 更新先チェックリスト（新運用）

### 5.1 開発者向け仕様書

- `manuals/implemented/03_Integrated_Data_Definitions.md`
  - 新しい `effect` 定義（今回なら `state_receive_bonus`）
  - effect JSON 拡張項目（例: `repeat_count`）の既定値・上限・エラー条件
- `manuals/implemented/08_Skill_Logic_Reference.md`
  - 処理順、条件判定、適用タイミング、consume対象
  - 反復実行系の順序（展開タイミング、1回ごとの判定、ログ仕様）
- `manuals/implemented/09_SelectResolve_Spec.md`
  - Select/Resolve で影響がある場合のみ更新

### 5.2 利用者向けマニュアル

- `manuals/implemented/01_Integrated_Player_Manual.md`
  - プレイヤー視点の効果説明
- `manuals/implemented/02_Integrated_GM_Creator_Manual.md`
  - 設定方法、入力例、裁定時注意
- `manuals/implemented/04_Character_Build_Guide.md`
  - ビルド選択に影響する場合のみ追記

### 5.3 データ正本（ユーザー更新）

- 用語図鑑データ（Glossaryシート）
  - 新語追加
  - 関連語リンク更新
- バフ/デバフ図鑑データ（Buff Catalogシート）
  - 名称、種別、説明、effect JSON
- スキルデータ正本
  - スキル効果、付与対象、タイミング、必要に応じて JSON 拡張列

### 5.4 テスト

- 単体テスト（震盪あり/なし、consume挙動）
- 仕様テスト（スキル定義lint、smoke）
- 既存回帰（破裂/亀裂まわり）
- JSON拡張時の lint 項目追加（例: `repeat_count` の型・範囲）

---

## 6. effect JSON 拡張時の追加ルール

`effects[]` の新フィールドや新記法（例: 同一効果複数回発動）を導入する場合は、以下を必須とする。

1. **03に仕様を明記**
   - フィールド名
   - 型
   - 既定値
   - 許容範囲
   - 異常値の扱い
2. **07に実行順を明記**
   - 展開タイミング
   - 1回ごとの条件判定有無
   - target再解決の有無
3. **lint/smoke を更新**
   - `tests/test_skill_catalog_smoke.py` の shape/field 検証
4. **上限を設定**
   - 大量展開で処理が不安定化しないよう、上限値を必ず持つ
5. **後方互換を明示**
   - 既存データが未指定でも従来動作になること
6. **データ正本の入力方法を明示**
   - シート上でどの列に入れるか
   - 文字列形式か JSON 形式か
   - 空欄時の扱い

---

## 7. 実装完了条件（Definition of Done）

以下を全て満たした時に「完了」とする。

1. 受け手側デバフとしてのロジック実装が完了
2. `APPLY_STATE` / `APPLY_STATE_PER_N` のテストが追加・更新済み
3. 03/07（必要なら08）へ仕様反映済み
4. 01/02（必要なら04）へユーザー説明反映済み
5. AI が、Glossary / Buff Catalog / スキル正本の更新方針を提示済み
6. ユーザーがシート側の更新を完了済み
7. ユーザーが通常のデータ更新フローで DB / キャッシュ反映を完了済み
8. 画面上で図鑑・説明・ゲーム内反映が確認済み
9. JSON拡張がある場合、03/07/lint とシート入力ルールの4点同期が完了

---

## 8. 既存提案（manuals/planned/03 系）への適用方針

### 8.1 既存案も同じルールで扱う

既存提案群:

- 行動阻害/封印/追加コスト
- フォールバック技
- 逆襲
- 気合い
- 経過ターン条件
- ランダムターゲット
- 同一効果の複数回発動 JSON 記法
- （今回）震盪（受け手側デバフ）

全てに対して、本書の更新先チェックリストとデータ更新方針を適用する。

### 8.2 優先順位

1. まず `03` と `07` を更新（仕様が曖昧なまま実装しない）
2. 影響がUI/宣言フローに及ぶ場合は `08` を更新
3. 実装とテストを完了
4. その後、ユーザー向け説明とシート更新案内を整備
5. 最後にユーザーがシート/DB反映を実施する

---

## 9. ユーザーへ渡す更新案内テンプレート

### 9.1 Glossary シート更新案内テンプレ

- シート: `Glossary`
- 更新対象行: `震盪` の新規追加
- 主な入力項目:
  - `ID`: 例 `W-SHINDOU`
  - `表示名`: `震盪`
  - `short`: `受ける破裂付与量を増やすデバフ`
  - `long`: `このデバフを受けた対象がスキル効果で破裂を付与される際、その増加量を+Nする`
  - `関連語`: `破裂, 状態異常, 付与`
- 更新後確認:
  - 用語リンクから short / long が正しく出るか

### 9.2 Buff Catalog シート更新案内テンプレ

- シート: `Buff Catalog`
- 更新対象行: `震盪` の新規追加
- 主な入力項目:
  - `名称`: `震盪`
  - `種別`: `debuff`
  - `説明`: `受ける破裂付与量 +N`
  - `effect JSON`: `state_receive_bonus` 定義
- 更新後確認:
  - バフ/デバフ図鑑表示
  - ツールチップ文言

### 9.3 スキル正本シート更新案内テンプレ

- 対象スキル行に `APPLY_BUFF` で `震盪` を付与する記述を追加
- 必要なら `repeat_count` などの新フィールド入力ルールも併記する
- 更新後確認:
  - 対象スキルが期待どおりに `震盪` を付与するか

### 9.4 DB / キャッシュ反映案内テンプレ

- ユーザーが通常のデータ更新フローを実行する
- スキルデータ、Glossary、Buff Catalog が反映されたことを確認する
- 必要なら再読込・キャッシュクリア・再取得を行う

---

本書は「震盪（受け手側デバフ）」導入時の設計・運用ガイドであり、同時に今後のスキル拡張全体に適用する更新プロトコルである。実装後は `03/07/08` の統合仕様へ確定反映し、図鑑系データについてはユーザーが正本シートと通常の反映フローを用いて更新すること。
