repos:
  - repo: local
    hooks:
      - id: encoding-guard
        name: encoding-guard (utf-8 without bom)
        entry: python scripts/check_text_encoding.py
        language: system
        pass_filenames: false
