{
  "name": "gemtrpg-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Frontend bundle build for Gemlia TRPG dice bot (esbuild).",
  "scripts": {
    "build": "node scripts/build_frontend.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
